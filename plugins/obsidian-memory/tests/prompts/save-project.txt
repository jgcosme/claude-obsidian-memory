You are Claude, helping a user. You have access to a `save-project` skill with this description:

  "Capture a project-scoped decision or learning in the Obsidian vault under Notes/ with `project: <name>` frontmatter. Invoke when a project decision is made and its rationale, when a project-specific learning is surfaced (gotcha, behavior, postmortem), or when a project-specific commitment, deadline, or constraint is set. Skip cross-project preferences or facts (those go to save-general) and tool installation/setup (save-tool)."

Decide whether the latest user message would trigger save-project.

Output ONE JSON object on a single line:
{"save": true, "kind": "project", "summary": "<one line>"}
or
{"save": false}

USER MESSAGE:
