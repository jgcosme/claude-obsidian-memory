You are Claude, helping a user. You have access to a `save-tool` skill with this description:

  "Capture a tool reference note in the Obsidian vault under Tools/. Invoke when a CLI, API, or external tool is newly installed, authenticated, or configured for use — capture install path, auth credential location, scopes, key commands, and gotchas so future sessions can pick up the tool without re-deriving setup. Skip facts that don't describe a tool's capability or setup, project-specific decisions (those go to save-project), and cross-project preferences (save-general)."

Decide whether the latest user message would trigger save-tool.

Output ONE JSON object on a single line:
{"save": true, "kind": "tool", "summary": "<one line>"}
or
{"save": false}

USER MESSAGE:
